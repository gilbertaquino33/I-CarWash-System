/**
 * EDIT THIS FILE to change the contact details shown on the website.
 * These are used by the landing page's "For shop owners" section and the footer.
 */

/** Email that shop owners contact when they want to join I-CarWash. */
export const OWNER_AGENT_EMAIL = "icarwash2026@gmail.com";

/** Optional. Leave as an empty string to hide the phone number everywhere. */
export const OWNER_AGENT_PHONE = "";

/** Name shown for the person/team owners will be talking to. */
export const OWNER_AGENT_NAME = "I-CarWash Team";

/** Where the shops are — shown in the footer. */
export const SERVICE_AREA = "Car wash shops around the Philippines";
